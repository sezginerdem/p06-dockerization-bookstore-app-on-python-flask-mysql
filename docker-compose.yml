version: "3.7"

services:
  database:
    image: mysql-5.7
    environment: 
      MYSQL_ROOT_PASSWORD: P123456p
      MYSQL_DATABASE: bookstore_db
      MYSQL_USER: sezgin
      MYSQL_PASSWORD: Sezgin_1
    networks:
      - clarusnet

  my-app:
    image: sezgin/bookstore:latest
    restart: always
    depends_on:
      - database
    ports: 
      - "80:80"
    networks:
      - clarusnet
      
networks:
  clarusnet:
    driver: bridge

    
    